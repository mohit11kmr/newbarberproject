org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Enable Gradle daemon to keep JVM warm between builds
org.gradle.daemon=true
# Allow parallel project execution where possible
org.gradle.parallel=true
# Configure on demand to reduce configuration time for multi-module projects
org.gradle.configureondemand=true
# Enable Gradle build cache to reuse build outputs
org.gradle.caching=true
android.useAndroidX=true
# Enable Kotlin incremental compilation for faster Kotlin builds
kotlin.incremental=true
